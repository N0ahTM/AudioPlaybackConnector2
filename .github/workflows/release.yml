name: Release

on:
  push:
    tags:
      - 'v[0-9]*'
  workflow_dispatch:
    inputs:
      tag:
        description: Existing release tag to publish, for example v1.2.3.
        required: true
        type: string

concurrency:
  group: audioplaybackconnector2-production-release
  cancel-in-progress: false

jobs:
  version:
    runs-on: ubuntu-24.04
    outputs:
      source_ref: ${{ steps.version.outputs.source_ref }}
      semver: ${{ steps.version.outputs.semver }}
      tag: ${{ steps.version.outputs.tag }}
      package_version: ${{ steps.version.outputs.package_version }}
    steps:
    - name: Checkout release tag
      uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      with:
        ref: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref }}
        fetch-depth: 0

    - name: Validate tag and exact checkout
      id: version
      shell: pwsh
      env:
        EVENT_NAME: ${{ github.event_name }}
        INPUT_TAG: ${{ inputs.tag }}
        REF_NAME: ${{ github.ref_name }}
      run: |
        $tag = if ($env:EVENT_NAME -eq 'workflow_dispatch') {
          $env:INPUT_TAG
        } else {
          $env:REF_NAME
        }
        if ($tag -notmatch '^v\d+\.\d+\.\d+$') {
          throw 'Release tag must look like v1.2.3.'
        }
        $head = (git rev-parse HEAD).Trim()
        $tagCommit = (git rev-parse "$tag^{commit}").Trim()
        if ($head -ne $tagCommit) {
          throw "Checked out commit $head does not match $tag ($tagCommit)."
        }
        $semver = $tag.TrimStart('v')
        foreach ($part in $semver.Split('.')) {
          if ([uint32]$part -gt [uint16]::MaxValue) {
            throw 'A release version component exceeds 65535.'
          }
        }
        "source_ref=$tag" >> $env:GITHUB_OUTPUT
        "semver=$semver" >> $env:GITHUB_OUTPUT
        "tag=$tag" >> $env:GITHUB_OUTPUT
        "package_version=$semver.0" >> $env:GITHUB_OUTPUT

  build-artifacts:
    needs: version
    uses: ./.github/workflows/build-release-artifacts.yml
    with:
      source_ref: ${{ needs.version.outputs.source_ref }}
      tag: ${{ needs.version.outputs.tag }}
      semver: ${{ needs.version.outputs.semver }}
      package_version: ${{ needs.version.outputs.package_version }}
      run_static_analysis: false
    secrets:
      SIGNING_CERTIFICATE_PFX: ${{ secrets.SIGNING_CERTIFICATE_PFX }}
      SIGNING_CERTIFICATE_PASSWORD: ${{ secrets.SIGNING_CERTIFICATE_PASSWORD }}

  create-draft:
    runs-on: ubuntu-24.04
    needs:
      - version
      - build-artifacts
    permissions:
      contents: write
    steps:
    - name: Download release artifacts
      uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4
      with:
        name: ${{ needs.build-artifacts.outputs.artifact_name }}
        path: release-artifacts

    - name: Build release body
      shell: pwsh
      run: |
        $changelog = [IO.File]::ReadAllText('release-artifacts/RELEASE_NOTES.md').Trim()
        $footer = @'

        ---

        ## Installation

        **Recommended:** Download and run `AudioPlaybackConnector2-WebSetup.exe`.

        For offline installation on x64 or ARM64, use the versioned `AudioPlaybackConnector2-Setup-${{ needs.version.outputs.semver }}.exe`.

        Both installers work per-user without administrator rights. Windows SmartScreen may warn because the setup executables are not yet code-signed.

        For App Installer, direct MSIX bundle installation, certificates, and dependency packages, see the [installation guide](https://github.com/N0ahTM/AudioPlaybackConnector2/blob/${{ needs.version.outputs.tag }}/docs/INSTALLATION.md).

        > The MSIX bundle is signed with a self-signed certificate. The setup executables are not yet code-signed.
        '@
        [IO.File]::WriteAllText('release-body.md', ($changelog + "`n" + $footer).Trim())

    - name: Create draft GitHub Release
      uses: softprops/action-gh-release@3bb12739c298aeb8a4eeaf626c5b8d85266b0e65 # v2
      with:
        tag_name: ${{ needs.version.outputs.tag }}
        name: v${{ needs.version.outputs.semver }}
        draft: true
        overwrite_files: true
        body_path: release-body.md
        files: |
          release-artifacts/*.msixbundle
          release-artifacts/*.appinstaller
          release-artifacts/*.exe
          release-artifacts/*.cer
          release-artifacts/Dependencies/**/*.appx
          release-artifacts/Dependencies/**/*.msix
        fail_on_unmatched_files: true

  publish-release:
    runs-on: ubuntu-24.04
    needs:
      - version
      - build-artifacts
      - create-draft
    if: ${{ github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: write
    environment:
      name: production-release
    steps:
    - name: Publish GitHub Release
      env:
        GH_TOKEN: ${{ github.token }}
      run: gh release edit '${{ needs.version.outputs.tag }}' --repo '${{ github.repository }}' --draft=false

    - name: Verify public release assets
      shell: pwsh
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        $release = gh release view '${{ needs.version.outputs.tag }}' --repo '${{ github.repository }}' --json assets,isDraft,isPrerelease | ConvertFrom-Json
        if ($release.isDraft -or $release.isPrerelease) {
          throw 'Release is not public and stable.'
        }
        $required = @(
          '${{ needs.build-artifacts.outputs.bundle_name }}',
          '${{ needs.build-artifacts.outputs.web_setup_name }}',
          '${{ needs.build-artifacts.outputs.offline_setup_name }}',
          'AudioPlaybackConnector2.appinstaller',
          'AudioPlaybackConnector2.cer'
        )
        $publishedNames = @($release.assets | ForEach-Object name)
        foreach ($name in $required) {
          if ($publishedNames -notcontains $name) { throw "Required release asset is missing: $name" }
        }
        foreach ($asset in $release.assets) {
          $reachable = $false
          for ($attempt = 1; $attempt -le 6; $attempt++) {
            try {
              Invoke-WebRequest -Uri $asset.url -Method Head -TimeoutSec 30 -ErrorAction Stop | Out-Null
              $reachable = $true
              break
            } catch {
              if ($attempt -eq 6) { throw }
              Start-Sleep -Seconds 10
            }
          }
          if (-not $reachable) { throw "Release asset is not publicly reachable: $($asset.name)" }
        }

  deploy-appinstaller:
    needs:
      - version
      - publish-release
    if: ${{ github.event_name == 'workflow_dispatch' }}
    permissions:
      contents: write
      pages: write
      id-token: write
    uses: ./.github/workflows/deploy-appinstaller.yml
    with:
      tag: ${{ needs.version.outputs.tag }}
      package_version: ${{ needs.version.outputs.package_version }}

  rollback-release:
    runs-on: ubuntu-24.04
    needs:
      - version
      - publish-release
      - deploy-appinstaller
    if: ${{ always() && needs.publish-release.result == 'success' && needs.deploy-appinstaller.result != 'success' }}
    permissions:
      contents: write
    steps:
    - name: Return GitHub Release to draft
      env:
        GH_TOKEN: ${{ github.token }}
      run: |
        gh release edit '${{ needs.version.outputs.tag }}' --repo '${{ github.repository }}' --draft=true
        test "$(gh release view '${{ needs.version.outputs.tag }}' --repo '${{ github.repository }}' --json isDraft --jq .isDraft)" = true
